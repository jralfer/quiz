<p>
    <% quizes.forEach(function(quiz){ %>
    <a href="/quizes/<%= quiz.id %>"><%= quiz.pregunta %></a>
    <a href="/quizes/<%= quiz.id %>/edit"><button>Editar</button></a>
    <form method="post" action="/quizes/<%= quiz.id %>?_method=delete">
        <button type="submit" onClick="return confirm('Borrar: <%= quiz.pregunta %>');">Borrar</button>
    </form>
    <br>
    <% })%>
</p>
<br>
<p>
<form method="get" action="/quizes/search">
    <p>Buscador de preguntas</p>
    <input type="text" name="search" id="search">
    <input type="submit" value="buscar">
</form>
</p>
<br>
<a href="/quizes/new"><button type="button">Crear pregunta</button> </a>